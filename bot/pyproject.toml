[project]
name = "weatherwear-bot"
version = "0.1.0"
description = "WeatherWear — Telegram bot for clothing recommendations based on street photos"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.20",
    "httpx>=0.28",
    "pydantic-settings>=2.12",
]
